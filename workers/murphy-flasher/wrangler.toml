name = "murphy-flasher"
main = "src/index.ts"
compatibility_date = "2026-05-05"

# Route to your custom domain
# Zone ID: Cloudflare Dashboard → 選域名 → 右側 API 區塊
# 格式為 32 位十六進制字串，例如 "1a2b3c4d5e6f7890abcdef1234567890"
routes = [
  { pattern = "murphy.pandacat.ai/*", zone_id = "YOUR_ZONE_ID" }
]

# R2 bucket for firmware binaries
[[r2_buckets]]
binding = "FIRMWARE_BUCKET"
bucket_name = "murphy-firmware"

# KV namespace for version metadata (small, fast access)
[[kv_namespaces]]
binding = "FIRMWARE_META"
id = "c0188469076f46a48cc0f08bb0982002"

[env.production]
routes = [
  { pattern = "murphy.pandacat.ai/*", zone_id = "YOUR_ZONE_ID" }
]
